
@{
    ViewBag.Title = "Test";
}

<style>
    #userList{
        float:left;
        height: 100%;
        min-height: 500px;
         width: 300px;
         overflow-x:hidden;
         overflow-y: auto;
         
         background-color:red;
    }
    #chatRoom {
        height: 70%;
        min-height: 500px;
        width: 80%;
        left: 300px;
        overflow-x: hidden;
        top: auto;
        overflow-y: auto;
        position: fixed;
        background-color: azure;
    }

    #inputBox {
        width: 80%;
        left: 300px;
        height: 18%;
        background-color: skyblue;
        position: fixed;
        bottom: 5px;
        border: 1px solid red;
    }

    #message {
        max-width: 1500px;
        width: 80%;
        height: 100%;
    }

    .floatLeft {
        float: left;
        height: 100%;
    }
</style>

<div id="userList">

</div>

<div id="chatRoom">

</div>

<div id="inputBox">

    <div class="floatLeft">
        <textarea id="message"></textarea>
    </div>
    <div class="floatLeft">
        <input type="button" id="sendmessage" value="Send" />
    </div>
</div>






<input type="hidden" id="displayname" value="Zhou Yang" />
@section scripts{
    <!--Script references. -->
    <!--Reference the jQuery library. -->
    <script src="~/Scripts/jquery-2.2.0.min.js"></script>
    <!--Reference the SignalR library. -->


    <script src="~/Scripts/jquery.signalR-2.1.2.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <!--<script src="http://localhost:8080/signalr/hubs"></script>-->
    <!--Add script to update the page and send messages.-->
    <!--<script type="text/javascript">
        $(function () {

            $.getScript("http://localhost:8080/signalr/hubs").done(function () {
                alert("Finished Download");

                $.connection.hub.url = "http://localhost:8080/signalr";
                // Declare a proxy to reference the hub.
                var chat = $.connection.myHub;
                // Create a function that the hub can call to broadcast messages.
                chat.client.addMessage = function (name, message) {
                    // Html encode display name and message.
                    var encodedName = $('<div />').text(name).html();
                    var encodedMsg = $('<div />').text(message).html();
                    // Add the message to the page.
                    $('#discussion').append('<li><strong>' + encodedName
                        + '</strong>:&nbsp;&nbsp;' + encodedMsg + '</li>');
                };
                // Get the user name and store it to prepend to messages.
                $('#displayname').val(prompt('Enter your name:', ''));
                // Set initial focus to message input box.
                $('#message').focus();

                // Start the connection.
                $.connection.hub.start().done(function () {
                    $('#sendmessage').click(function () {
                        // Call the Send method on the hub.
                        chat.server.send($('#displayname').val(), $('#message').val());
                        // Clear text box and reset focus for next comment.
                        $('#message').val('').focus();
                    });
                });
            });

            //Set the hubs URL for the connection

        });
    </script>-->

    <script type="text/javascript">
        $(function () {



            var i = 0;
            
            var connection = $.hubConnection("http://localhost:9000/signalr", { useDefaultPath: false });

            var proxy = connection.createHubProxy('agentHub');

            var myInfo = { name: "Zhou Yang" };
            alert(myInfo.name);

            proxy.on('addMessage', function (name, msg) {

                // Html encode display name and message.
                var encodedName = $('<div />').text(name).html();
                var encodedMsg = $('<div />').text(msg).html();
                //// Add the message to the page.
                $('#chatRoom').append('<li id="msg' + i + '"><strong>' + encodedName
                    + '</strong>:&nbsp;&nbsp;' + encodedMsg + '</li>');


                $('#chatRoom').scrollTop($('#msg' + i).position().top);
                    
                i++;
                

            });


            // Get the user name and store it to prepend to messages.
            //$('#displayname').val(prompt('Enter your name:', ''));

            // console.log($('#displayname').val());
            // Set initial focus to message input box.
            //$('#message').focus();

            function sendMsg() {
                proxy.invoke('sendMessage', $('#displayname').val(), $('#message').val());

                // Clear text box and reset focus for next comment.
                $('#message').val('').focus();
            }


            function userInfo(name,url,logintime) {
                this.name = firstname;
                this.url = url;
                this.logintime = logintime;
            }

            connection.start().done(function () {

                $('#sendmessage').click(function () {
                    sendMsg();
                });

                $(document).keypress("enter", function (e) {
                    if (e.ctrlKey)
                        sendMsg();
                });
            });


        });
    </script>

}


